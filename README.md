# CoffeeShopMenu

The purpose of this project is to display a menu system for a minimalistic coffee shop. The goal was to simulate a kiosk menu so that the shop doesn't have to pay someone to specifially take orders behind the counter and minimalize overhead.